export type MeetupProps = {
    id: number,
    topic: string
    description: string,
    location: string,
    date: string,
    time: string,
    image_url: string,
    archived: boolean,
    created_at: string,
    updated_at: string,
    file?: any
}

// export default MeetupProps;
